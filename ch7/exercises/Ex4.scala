/*
    4. Make two source files, each contributing two classes and a 
    top-level function to a given package. What class files are generated? 
    In which directories? What happens if you move the source files to 
    different directories? What happens if you rename the source files?
*/

@main def ex4 =

    val laptop = store.Laptop(16, 200)
    println(store.getVehicles)